import 'package:flutter/widgets.dart';

class Task325Screen extends StatelessWidget {
  const Task325Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const Center(child: Text('Task 3.25 Screen'));
}
