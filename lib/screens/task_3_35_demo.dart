import 'package:flutter/widgets.dart';

class Task335Demo extends StatelessWidget {
  const Task335Demo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const Center(child: Text('Demo 3.35'));
}
