import 'package:flutter/widgets.dart';

class Task340Footer extends StatelessWidget {
  const Task340Footer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const Text('Footer - Task 3.40');
}
