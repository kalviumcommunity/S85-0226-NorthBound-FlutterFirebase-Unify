import 'package:flutter/widgets.dart';

class Task322Widget extends StatelessWidget {
  const Task322Widget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const Center(child: Text('Task 3.22'));
}
