import 'package:flutter/widgets.dart';

class Task323Widget extends StatelessWidget {
  const Task323Widget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const Padding(
        padding: EdgeInsets.all(8.0),
        child: Text('Task 3.23'),
      );
}
