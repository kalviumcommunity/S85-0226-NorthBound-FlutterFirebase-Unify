import 'package:flutter/widgets.dart';

class Task334Loading extends StatelessWidget {
  const Task334Loading({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const Center(child: Text('Loading... Task 3.34'));
}
