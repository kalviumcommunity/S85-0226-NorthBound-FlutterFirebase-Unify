import 'package:flutter/widgets.dart';

class Task341Header extends StatelessWidget {
  const Task341Header({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const Text('Header - Task 3.41');
}

